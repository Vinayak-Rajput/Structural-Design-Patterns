Modernizing a Legacy Payment Gateway

Your e-commerce app relies on a standard PaymentProcessor interface with a pay(double dollars) method.
You need to integrate a third-party payment vendor whose SDK uses sendPayment(int cents, String currencyCode) instead.

Task: Build an adapter class that implements PaymentProcessor and translates calls into the incompatible vendor SDK format
      without altering either existing code base.